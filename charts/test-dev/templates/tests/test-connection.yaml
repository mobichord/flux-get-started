apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-dev.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "test-dev.name" . }}
    helm.sh/chart: {{ include "test-dev.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "test-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
